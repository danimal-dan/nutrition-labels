version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - echo "Installing dependencies..."
      - cd webapp
      - npm install --prefer-offline
  pre_build:
    commands:
      - echo "Running tests (write some tests dummy)..."
artifacts:
  files:
    - dist/**/*
  base-directory: dist
cache:
  paths:
    - 'node_modules/**/*'
